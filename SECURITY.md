# Security Policy

Do not open public issues or commits containing credentials, tokens, private webhook URLs, or downloaded runtime payloads that were not intended for publication.

If sensitive information is accidentally committed, rotate the credential first, then remove it from history before making the repository public.

This project is currently pre-production. Use GitHub private security reporting after the repository is published if available.
