{
  "name": "chitan-watch",
  "private": true,
  "version": "0.1.0",
  "description": "Chitan Watch monorepo bootstrap",
  "scripts": {
    "web:dev": "cd apps/web && python3 -m http.server 4173",
    "serve": "PYTHONPATH=crawler python3 -m chitan_watch.cli serve --store-dir storage/chitan-watch --web-dir apps/web",
    "test:py": "PYTHONPATH=crawler python3 -m unittest discover -s tests"
  }
}
